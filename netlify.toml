[build]
  base = "frontend"
  command = "npm ci --include=dev && npm run build"
  publish = "dist"

[build.environment]
  VITE_API_URL = "https://maktab-17.onrender.com"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
